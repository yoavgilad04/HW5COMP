//non-void return statements
int foo(bool c, int x, byte y) {
	int nx = x;
	int ny = y;
    if (not c) {
        print("already? :(");
        return 15;
    }
    print("ok");
    while (nx < 10) {
        nx = nx + 1;
        if (c and nx == 7) {
            printi(nx);
            print("alright");
            return 17;
        }
    }
    while (ny > 12b) {
        ny = ny - 1b;
        printi(ny);
        print("back!");
        return 18;
    }
    print("here!");
    return 322;
}

bool opposite(bool a) {
    while (true) {
        while (true) {
            while(true) {
                while(true) {
                    while (true) 
                        return not a;
                        break;
                }
            }
        }
    }
    return not a;
}

byte multiply(byte a, byte c) {
    return a*c;
}

void main() {
    printi(foo(false,5,15b));
    printi(foo(true,5,21b));
    printi(foo(true,11,4b));
    printi(foo(true,10,13b));
    if (opposite(false)) {
        print("great");
    }
    printi(multiply(16b,16b));
}