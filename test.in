override int foo(int x, int z)
{
    while(x>5)
    {
        x = x + 1;
    }
}

void main()
{
    bool x = true or false or 6 > 8;
    //bool x = not true;
    int w = foo(6,7);
    print("my name is melissa");
}